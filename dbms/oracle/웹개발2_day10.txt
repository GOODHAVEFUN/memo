day10

class
설계도 
필드 객체의 특징 속성
생성자 
메소드 동작


객체 
클래스에 구체화
하나의 클래스로 여러개의 객체를 만들수 있다
메모리에 직접 할당


복습 -> join ->VIEW

GROUP BY , HAVING
	GROUP BY 절은 특정 컬러에 있는값을 묶어서 데이터 집계
	SELECT WHERE GROUP BY HAVING ORDER BY 

	FROM>ON>JOIN>WHERE > GROUP BY > HAVING > SELECT > ORDER BY
